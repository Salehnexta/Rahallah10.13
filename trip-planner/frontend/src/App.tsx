import React from 'react';
import ChatContainer from './components/ChatContainer';

/**
 * Main App component
 */
const App: React.FC = () => {
  return (
    <div className="app">
      <ChatContainer />
    </div>
  );
};

export default App;
